# Licenses
This directory is empty but is reserved for license files.

Other scripts expects license files named `bloom.license` and `gds.license` to be present here when deploying Bloom and GDS respectively.